{% extends 'base.html.twig' %}
    {% block title %}Update Profile{% endblock %}
    {% block main %}
        <div class="offset-1">
            <p>Your pseudo : {{ user.pseudo }}</p>
            <p>Your mail address : {{ user.email }}</p>
            <p>Your avatar : </p><img class="user-img-update" src="{{ asset('uploads/images/'~user.picture) }}" alt="">
        </div>
            <div class=" form text-center offset-4 col-4">
            <h2>You can update your profile here</h2>
                {{ form(form) }}
            </div>
    {% endblock %}
    {% block footer %}

    {% endblock %}